<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- View background color -->
    <solid android:color="@color/colorTertiaryText"></solid>

    <!-- View border color and width -->
    <stroke
        android:width="1dp"
        android:color="@color/colorTertiaryText"></stroke>

    <padding
        android:bottom="@dimen/padding_box_symptome"
        android:left="@dimen/padding_box_symptome"
        android:right="@dimen/padding_box_symptome"
        android:top="@dimen/padding_box_symptome" />

    <!-- The radius makes the corners rounded -->
    <!--    <corners
            android:radius="2dp"   >
        </corners>-->

</shape>